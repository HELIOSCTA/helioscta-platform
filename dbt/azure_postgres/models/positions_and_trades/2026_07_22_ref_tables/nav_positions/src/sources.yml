version: 2

sources:
  - name: nav
    schema: nav
    description: NAV source tables for the active positions/trades model.
    tables:
      - name: positions
        description: Raw NAV position rows at fund/date/upload/file-row grain.
