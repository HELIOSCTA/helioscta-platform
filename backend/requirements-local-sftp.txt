# Optional local SFTP workflow dependencies.
#
# Install these only on hosts that run local SFTP pulls such as NAV positions.
# Do not install this file on the Linux production VM unless a VM-hosted SFTP
# workflow is explicitly promoted later.

-r requirements.txt
openpyxl==3.1.5
paramiko==3.5.1
