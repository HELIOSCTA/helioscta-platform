# Optional local Windows-only dependencies for ICE Python workflows.
#
# Install these only on the licensed Windows host that has ICE XL / ICE Python.
# Do not install this file on the Linux production VM.

-r requirements.txt
pywin32==306

# The proprietary ICE Python package is supplied by the licensed ICE XL
# installation and is not available from PyPI. Install that wheel locally on
# the Windows service host before enabling ICE workflows.
